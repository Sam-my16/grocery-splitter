rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone signed in (anonymous auth included) can read/write bills.
    // Good enough for a 3-person household app; not meant for public data.
    match /bills/{billId} {
      allow read, write: if request.auth != null;
    }
  }
}
